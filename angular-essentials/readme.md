# Rodando o projeto com `http-server`
* utilizado para resolver o problema de `CORS` devido ao uso da diretiva `ngInclude`

* instalando o `http-server`


        npm install http-server -g

* executando        


        http-server